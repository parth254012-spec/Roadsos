kind = "web"
previewPath = "/"
title = "RoadSoS"
version = "1.0.0"
id = "artifacts/roadsos"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 20383

[services.development]
run = "pnpm --filter @workspace/roadsos run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/roadsos", "run", "build" ]
publicDir = "artifacts/roadsos/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "20383"
BASE_PATH = "/"
